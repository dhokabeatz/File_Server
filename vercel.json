{
    "builds": [{
        "src": "FileServer/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "FileServer/(.*)",
            "dest": "FileServer/wsgi.py"
        }
    ]
}
